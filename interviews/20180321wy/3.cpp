#include <iostream>
#include <string>
using namespace std;

char * getTree(string in, char * tree[]){
	
}

int main(){
	int N;
	cin >> N;
	char *a[N] = {NULL};
	string input;
	cin >> input;
	getTree(input, a);
	return 0;
}